# Korean Prosody Experiment

Static GitHub Pages version of the sequential one-repetition experiment.

Open `index.html` through GitHub Pages. The page uses:

- `experiment.css`
- `experiment.js`
- `experiment_data_sequential_1rep.js`
- `extract/*.wav`

The experiment order is sequential: `q0101` through `q1005`, one repetition.
